# Firestore Collections

- `loans_cache`: Stores cached mortgage data for standard and first-time buyers.
- `scrape_history`: Historical snapshots persisted every scrape for analytics and fallbacks.
- `error_logs`: Capture scraping and API errors for observability.
- `webhook_subscriptions`: External subscriber registry for rate-change notifications.
